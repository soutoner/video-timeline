## Preparations

### Requirements

`node, npm, git`

`npm install -g bower`

`npm install -g grunt-cli`

### Download dependencies

`npm install`

`bower install`

### Compile SASS and CoffeeScript

`grunt`